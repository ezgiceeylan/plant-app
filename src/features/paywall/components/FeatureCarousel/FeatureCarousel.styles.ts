import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  list: {
    marginHorizontal: -24,
    marginBottom: 24,
    marginTop: 20,
  },
  content: {
    paddingHorizontal: 24,
    gap: 8,
  },
});
