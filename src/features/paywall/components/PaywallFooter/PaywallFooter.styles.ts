import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  footer: {
    gap: 10,
    marginTop: 8,
  },
  note: {
    color: '#FFFFFF85',
    textAlign: 'center',
    fontSize: 9,
  },
  links: {
    flexDirection: 'row',
    justifyContent: 'center',
  },
  link: {
    color: '#FFFFFF80',
  },
});
